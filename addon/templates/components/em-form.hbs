{{#if errors}}
  <div class="alert alert-warning">
    <ul>
      {{#each errors as |error|}}
        <li>{{ error.message.detail }}</li>
      {{/each}}
    </ul>
  </div>
{{/if}}
{{yield this}}
{{#if submitButton}}
  {{em-form-submit mainComponent=mainComponent}}
{{/if}}
